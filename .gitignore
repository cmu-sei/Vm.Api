bin/
obj/
player/

# Test run output. `dotnet test --results-directory TestResults` writes here, as CI does.
TestResults/

# Coverage output. `scripts/coverage.sh` writes the raw cobertura files and the generated report
# here, and both are regenerated from scratch on every run.
coverage/
*.user
*.csproj.lscache
player_vm.db
.vs/
appsettings.Development.json
.vscode/

# Node client build artifacts (generated by nswag)
node_modules/
src/Player.Vm.Api.Client/swagger.json
